delete from gha_events where created_at > '{{from}}' and created_at < '{{to}}';
delete from gha_assets where dup_created_at > '{{from}}' and dup_created_at < '{{to}}';
delete from gha_branches where dup_created_at > '{{from}}' and dup_created_at < '{{to}}';
delete from gha_comments where dup_created_at > '{{from}}' and dup_created_at < '{{to}}';
delete from gha_commits where dup_created_at > '{{from}}' and dup_created_at < '{{to}}';
delete from gha_commits_files where dt > '{{from}}' and dt < '{{to}}';
delete from gha_computed where dt > '{{from}}' and dt < '{{to}}';
delete from gha_events_commits_files where dup_created_at > '{{from}}' and dup_created_at < '{{to}}';
delete from gha_forkees where dup_created_at > '{{from}}' and dup_created_at < '{{to}}';
delete from gha_issues where dup_created_at > '{{from}}' and dup_created_at < '{{to}}';
delete from gha_issues_labels where dup_created_at > '{{from}}' and dup_created_at < '{{to}}';
delete from gha_issues_events_labels where created_at > '{{from}}' and created_at < '{{to}}';
delete from gha_issues_pull_requests where created_at > '{{from}}' and created_at < '{{to}}';
delete from gha_logs where dt > '{{from}}' and dt < '{{to}}';
delete from gha_milestones where dup_created_at > '{{from}}' and dup_created_at < '{{to}}';
delete from gha_pages where dup_created_at > '{{from}}' and dup_created_at < '{{to}}';
delete from gha_parsed where dt > '{{from}}' and dt < '{{to}}';
delete from gha_payloads where dup_created_at > '{{from}}' and dup_created_at < '{{to}}';
delete from gha_pull_requests where dup_created_at > '{{from}}' and dup_created_at < '{{to}}';
delete from gha_releases where dup_created_at > '{{from}}' and dup_created_at < '{{to}}';
delete from gha_skip_commits where dt > '{{from}}' and dt < '{{to}}';
delete from gha_teams where dup_created_at > '{{from}}' and dup_created_at < '{{to}}';
delete from gha_texts where created_at > '{{from}}' and created_at < '{{to}}';
