PG_USER=gha_admin db.sh psql graphql
select name from sprojects_health where series = 'phealthgraphqlspec' and name not in (select name from sprojects_health where series = 'phealthgraphiql');
select name from sprojects_health where series = 'phealthgraphqlspec' and name not in (select name from sprojects_health where series = 'phealthgraphqljs');
runq metrics/shared/projects_health.sql {{exclude_bots}} "`cat util_sql/exclude_bots.sql`" | grep 'PRs: Number of PRs opened in the last 12 months'
select * from sprojects_health where series = 'phealthgraphiql' and name = 'PRs: Number of PRs opened in the last 12 months';
